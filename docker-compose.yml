version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: laremise_db
    restart: always
    environment:
      POSTGRES_USER: SDN
      POSTGRES_PASSWORD: SDN
      POSTGRES_DB: laremise
    ports:
      - "5460:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: